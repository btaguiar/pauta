# Pauta — copie para .env e preencha. Nunca comite o .env.

# Acesso aos modelos via gateway OpenRouter, que fala o protocolo da OpenAI.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
TAVILY_API_KEY=

# Modelos por papel (ADR 007). Nenhum default no código.
# Use o id completo do gateway, com a casa de origem no nome.
#
# ESTA ESCOLHA AINDA NÃO FOI MEDIDA. Preencha eval/matrix.json e rode
# `uv run python eval/matrix.py` para comparar barato contra caro em cada papel
# sobre o mesmo golden set. Quando escolher, troque este bloco pelo número que
# justificou a escolha e registre uma ADR no ARCHITECTURE.md. Comentário sem
# número aqui é palpite com aparência de decisão.
MODEL_WORKER=
MODEL_ROUTER=
MODEL_CRITIC=
EMBEDDING_MODEL=

# Juiz do eval: precisa ser de um provider DIFERENTE do executor, para evitar
# viés de auto-preferência. Pelo gateway, isso é só trocar a casa no id do
# modelo, com a mesma OPENROUTER_API_KEY.
JUDGE_MODEL=

# Banco (checkpointer e contador de orçamento diário).
# Credencial de desenvolvimento local, idêntica à do docker-compose.yml.
# Se mudar aqui, mude lá no mesmo commit. Em produção vem do ambiente.
DATABASE_URL=postgresql://pauta:pauta@localhost:5432/pauta

# Comportamento do grafo
HITL_MODE=auto
MAX_SUPERVISOR_STEPS=8
MAX_CRITIC_LOOPS=2
# Voltas do research sem achar nada antes de a run desistir e o writer dizer
# o que faltou. Medido: sem teto, uma tarefa sem resposta no corpus repetiu
# research 4 vezes e gastou 58 mil tokens sem produzir briefing.
MAX_EMPTY_RESEARCH=2
BUDGET_TOKENS_PER_RUN=60000
# Medido: research leva ~29s com 3 buscas; supervisor leva ~1,6s.
NODE_TIMEOUT_SUPERVISOR_S=20
NODE_TIMEOUT_RESEARCH_S=90
NODE_TIMEOUT_WRITER_S=45
NODE_TIMEOUT_CRITIC_S=45
NODE_TIMEOUT_ANALYST_S=90
NODE_RETRIES=2
MAX_TOOL_ROUNDS=3

# Retriever
RETRIEVER_TOP_K=5
CHUNK_SIZE=800
CHUNK_OVERLAP=100

# Preço por milhão de tokens da combinação que você mediu. Vazio significa
# que o relatório mostra tokens e não calcula custo.
COST_PER_MTOK_USD=

# Guardrails da API pública
DAILY_BUDGET_USD=5.00
RATE_LIMIT_PER_IP=3/hour

# Trace opcional, desligado por padrao
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=pauta
